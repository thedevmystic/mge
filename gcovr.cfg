[gcovr]
root = .
object-directory = build/engine

# Only include source files under engine/
filter = engine/

# Explicitly exclude deps and other build artifacts
exclude = build/_deps/
exclude = build/(?!engine)

# Output options
sonarqube = coverage.xml
